const simulations={healthy:{status:"PASS · CLEAR",explanation:"The response satisfies the hard contract and raises no statistical review signal.",claim:"This controlled response satisfies protocol version 1.0.0."},schema:{status:"FAIL · CLEAR",explanation:"A required field is absent. Downstream meaning can no longer be reconstructed safely.",claim:"The response violates the declared schema contract."},identity:{status:"FAIL · CLEAR",explanation:"The series denominator changed. A valid number would now describe a different quantity.",claim:"The response no longer represents the pinned series identity."},late:{status:"FAIL · CLEAR",explanation:"After the grace period, the latest expected TARGET-day observation is missing.",claim:"The response violates the predeclared freshness contract at this check time."},spike:{status:"PASS · REVIEW",explanation:"Structure and freshness remain valid, but the synthetic latest return crosses the robust review threshold.",claim:"The observation warrants inspection; the signal alone does not establish an error."}};
for(const input of document.querySelectorAll('input[name="fault"]'))input.addEventListener("change",()=>{const item=simulations[input.value];document.querySelector("#sim-status").textContent=item.status;document.querySelector("#sim-explanation").textContent=item.explanation;document.querySelector("#sim-claim").textContent=item.claim});
const fmt=value=>value?new Intl.DateTimeFormat("en-GB",{dateStyle:"medium",timeStyle:value.includes("T")?"short":undefined}).format(new Date(value)):"—";
fetch("data/latest.json").then(response=>{if(!response.ok)throw new Error("No recorded run");return response.json()}).then(report=>{const contract=report.contract_status.toUpperCase(),review=report.review_status.toUpperCase();document.querySelector("#contract-status").textContent=contract;document.querySelector("#review-status").textContent=review;document.querySelector("#latest-date").textContent=report.latest_observation;document.querySelector("#row-count").textContent=report.observation_count;document.querySelector("#checked-at").textContent=fmt(report.checked_at);document.querySelector("#headline-finding").textContent=contract==="PASS"?`The latest recorded response satisfied every hard contract check; review status: ${review.toLowerCase()}.`:"The latest recorded response violated at least one hard contract check.";document.querySelector("#live-note").textContent=report.findings.length?report.findings.map(item=>item.message).join(" "):"No findings were emitted by the latest recorded run."}).catch(()=>{document.querySelector("#live-note").textContent="No complete live run has been recorded yet; the deterministic validation lab remains available."});
fetch("data/evidence-index.json").then(response=>response.ok?response.json():Promise.reject()).then(index=>{document.querySelector("#prospective-runs").textContent=index.prospective_run_count}).catch(()=>{});
